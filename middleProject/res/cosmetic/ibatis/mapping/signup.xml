<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="signup">

<!-- name,id,password만 입력해서 회원가입 -->
	<insert id="insertMember" parameterClass="memberVO">
	insert all 
    into member (mem_name, mem_id, mem_pass)
    values(#mem_name#,#mem_id#,#mem_pass#) 
    into mem_detail(mem_id,mem_cupon) 
    values (#mem_id#,#mem_cupon#) 
    SELECT * FROM DUAL
	</insert>


<!-- 회원id로 중복체크  -->
<select id="countMemberId" parameterClass="String" resultClass="int">
	select count(*) from member where mem_id = #mem_id#
</select>


	
</sqlMap>
	
